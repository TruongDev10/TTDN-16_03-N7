<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Extend Project Form View -->
    <record id="view_project_project_form_inherit_cost" model="ir.ui.view">
        <field name="name">project.project.form.inherit.cost</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project_core.view_project_project_form"/>
        <field name="arch" type="xml">
            <!-- Thêm button box cho chi phí -->
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_costs" type="object" class="oe_stat_button" icon="fa-money" context="{'default_project_id': id}">
                    <field name="cost_count" widget="statinfo" string="Chi phí"/>
                </button>
            </xpath>
            
            <!-- Thêm field tổng chi phí -->
            <xpath expr="//group[field[@name='description']]" position="after">
                <group string="Chi phí">
                    <field name="total_cost" readonly="1"/>
                    <button name="action_compute_cost_from_timesheet" string="Tính chi phí từ Timesheet" type="object" class="btn-secondary"/>
                </group>
            </xpath>
            
            <!-- Thêm tab chi phí -->
            <xpath expr="//notebook" position="inside">
                <page string="Chi phí" name="costs">
                    <field name="cost_line_ids" nolabel="1" context="{'default_project_id': id}">
                        <tree string="Chi phí">
                            <field name="date"/>
                            <field name="task_id"/>
                            <field name="employee_id"/>
                            <field name="hours"/>
                            <field name="hourly_rate"/>
                            <field name="cost_amount" sum="Tổng"/>
                            <field name="state" widget="badge"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
